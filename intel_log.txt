# بدلاً من الكتابة في ملف، سنعرض البيانات حية في الصفحة لضمان استقرار السيادة
def log_intel(data):
    # في بيئة Vercel، نستخدم السجلات (Logs) بدلاً من الملفات النصية
    print(f"INTEL_LOG: {data}") 
